# The ghostty submodule is upstream Zig source, not authored here.
ghostty/** linguist-vendored

# Vendored crates inside the Rust workspace are not authored here either.
cmux-tui/vendor/** linguist-vendored

# Keep shell scripts and packaging metadata LF regardless of platform.
*.sh text eol=lf
*.spec text eol=lf
PKGBUILD text eol=lf
